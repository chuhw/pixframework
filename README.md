Pix Framework 為 PIXNET 所開發使用的 PHP Framework
(官方已不 maintain)

## 安裝 - Installation ##
### 使用 Composer ###
- 增加 `"chuhw/pixframework": "dev-master"` 到您的 `composer.json` 的 `require` 部分.
- 執行 `composer install`.

